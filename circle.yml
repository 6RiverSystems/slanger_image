machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

test:
  post:
    - make build

deployment:
  gke:
    branch: feature/gke
    commands:
      - make gcloud_deploy


